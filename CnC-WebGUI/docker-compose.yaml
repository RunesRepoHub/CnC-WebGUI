version: "3.2"
services:
  cnc-webgui-db:
    image: 192.168.1.140:5000/cnc-mysql:1.0 
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=12Marvel
      - MYSQL_DATABASE=machines
      - MYSQL_ROOT_HOST=%
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - ~/CnC-WebGUI/src:/cnc-webgui/db
  web:
    image: 192.168.1.140:5000/cnc-web:1.0
    environment:
      - MYSQL_ROOT_PASSWORD=12Marvel
      - MYSQL_USER=root
      - MYSQL_PASSWORD=12Marvel
      - MYSQL_DATABASE=machines
      - MYSQL_HOST=db
    restart: always
    ports:
      - "8080:80"
    depends_on:
      - cnc-webgui-db
    volumes:
      - ~/CnC-WebGUI/src:/cnc-webgui/web